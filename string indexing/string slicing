#string slicing
#extracting sub-string from string is called slicing

word = ("table tennis")
print (word [6:12])
#this goes from index 6 to 12 which is the same as last string
print (word [6: ])
#leaving the end of range blank will tell python to print up until
#the last index

print (word [ : ])
